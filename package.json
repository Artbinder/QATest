{
  "name": "artbinder-qa-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.0.0",
    "xlsx": "^0.18.5"
  }
}
